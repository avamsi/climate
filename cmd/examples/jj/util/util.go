// util is just a proof of concept to show climate works across directories /
// packages / files. There's otherwise no reason to have a util package.
package util

// Infrequently used commands such as for generating shell completions.
type Util struct{}
